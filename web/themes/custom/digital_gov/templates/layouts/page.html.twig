{#
/**
* @file
* Theme override to display a single page.
*
* The doctype, html, head and body tags are not in this template. Instead they
* can be found in the html.html.twig template in this directory.
*
* Page content (in order of occurrence in the default page.html.twig):
* - node: Fully loaded node, if there is an automatically-loaded node
* associated with the page and the node ID is the second argument in the
* page's path (e.g. node/12345 and node/12345/revisions, but not
* comment/reply/12345).
*
//these are the regions for digital.gov
  page.header: 'Header'
  page.primary_menu: 'Primary menu'
  page.secondary_menu: 'Secondary menu'
  page.page_top: 'Page top'
  page.page_bottom: 'Page bottom'
  page.featured: 'Featured'
  page.breadcrumb: 'Breadcrumb'
  page.content: 'Content'
  page.sidebar_first: 'Sidebar first'
  page.sidebar_second: 'Sidebar second'
  page.footer: 'Footer'
*
* @see template_preprocess_page()
* @see html.html.twig
*/
#}
    <div class="paper">
        {# what is this notice-bar? #}
      {#{{- partialCached "core/notice-bar.html" . -#}}
      {% include 'themes/custom/digital_gov/templates/layouts/core/usa-banner.html'%}
      {#{{- partialCached "core/sitewide-alert.html" . -}}
      [Image Placeholder] {# {{ drupal_menu('info') }} 
      [Info Menu Placeholder] {# {{ drupal_menu('info') }} 
      {{- partialCached "core/header.html" . -}}
      {#cathy, check if thee image and the menus are in the header"}

      {{/* Main Content */}}
      {{ block "data" . }}{{ end }}
      {{ block "content" . }}The BASE CONTENT didn't load. :/{{ end }

      {{- partialCached "core/newsletter-signup.html" . -}}
      {{- partialCached "core/footer.html" . -}}
      {{- partialCached "core/usa-identifier.html" . -}}#}

    </div>
    <!-- end paper -->

    <div id="clicks_iframe" class="display-none"></div>
  </body>

  {# you are just going to go through each of these files and add them.#}

